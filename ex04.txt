#include <stdio.h>
int inverter(int n){
  int invertido=0;
  while(n!=0){
    invertido=invertido*10+(n%10);
    n/=10;
  }
return invertido;
}
int main(){
  int a;
  printf("Digite um número:");
  scanf("%d",&a);
  int invertido=inverter(a);
  printf("Número invertido:%d",invertido);
  return 0;
}